<Page
    x:Class="ConnectionMgr.Views.Pages.HostsFileEditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="clr-namespace:ConnectionMgr.Helpers"
    xmlns:local="clr-namespace:ConnectionMgr.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="HostsFileEditPage"
    d:DataContext="{d:DesignInstance local:HostsFileEditPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">
    <Page.Resources>
        <helpers:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />
		<BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Page.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<!--- Pseudo-Menu -->
		<StackPanel Grid.Row="0"
					Orientation="Horizontal">
			<ui:Button Content="Save"
					   ToolTip="Save changes to Hosts file"
					   Command="{Binding ViewModel.RefreshConnectionCommand}"
					   Icon="{ui:SymbolIcon DocumentSave24}" />
			<ui:Button Content="Cancel"
					   ToolTip="Cancel pending changes"
					   Command="{Binding ViewModel.RefreshConnectionCommand}"
					   Margin="4,0,0,0"
					   Icon="{ui:SymbolIcon DismissCircle24}" />
			<!-- ProgressRing -->
			<ui:ProgressRing IsIndeterminate="True"
						 Height="28" Width="28"
						 Margin="36,0,0,0"
						 Visibility="{Binding ViewModel.ShowProgress, Converter={StaticResource BoolToVis}}" />
		</StackPanel>

		<!-- Editor -->
		<Grid Grid.Row="1">
			<TextBox HorizontalAlignment="Stretch"
						VerticalAlignment="Stretch"
						Margin="2,18,2,12"
						FontFamily="Consolas"
						AcceptsReturn="True"
						TextWrapping="Wrap" 
						VerticalScrollBarVisibility="Visible"/>
		</Grid>

	</Grid>

</Page>
